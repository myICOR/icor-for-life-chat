{
  "name": "icor-for-life-chat",
  "version": "0.5.2",
  "private": true,
  "description": "ICOR for Life - AI Chat - the ICOR AI team inside your Obsidian vault.",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs",
    "gate": "npm run typecheck && npm run build && npm run lint && npm test",
    "pretest": "node test/build.mjs",
    "lint": "eslint src styles.css"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.226"
  },
  "devDependencies": {
    "@eslint/css": "^1.4.0",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.39.5",
    "eslint-plugin-obsidianmd": "^0.4.2",
    "obsidian": "^1.8.7",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.68.0"
  }
}
